# Pixlr Express 非 AI 功能还原矩阵

> 参考取证：2026-07-13，Pixlr Express 桌面编辑器 `1920 × 878`、已打开栅格图片状态。只还原通用布局、交互层级和非 AI 能力；不复制 Pixlr 品牌、文案、插画、用户头像或素材。

状态定义：`可复用` 表示 miniPaint 已有内核能力但尚未进入 Pixlr 式壳层；`缺失` 表示需要新增实现；`排除` 为用户明确不要、不得显示或请求的能力。

## 0. 编辑器工作台（第一优先级）

| 编号 | 参考状态/入口 | 本地目标 | 当前状态 | 验收证据 |
| --- | --- | --- | --- | --- |
| W-01 | 56px 左侧深色图标工具轨 | 固定图标轨、悬停中文 tooltip、单一激活工具 | 部分可用：10 个非 AI 入口已建立，工具面板路由已接通；同状态对照后，激活态已改为中性深灰底色，不使用参考中未出现的青色侧边条 | 1920×878 截图、Chromium E2E |
| W-02 | 中心黑色画布工作台 | 居中画布、棋盘透明背景、图片适配缩放 | 部分可用：深色工作区与居中画布已替换；已在固定 3840×2880 样图、26% 缩放、1920×878 视口下建立视觉基准 | 固定 PNG Canvas 状态 + `editor-loaded` 视觉快照 |
| W-03 | 右侧窄图层轨（添加、缩略图、选中、锁定） | 图层缩略图、选择、显隐、排序、添加与锁定 | 部分可用：窄图层轨、添加、选择/显隐/删除、导入图片缩略图、锁定与收起已实现；图层重命名和更完整排序入口仍待补 | 图层 E2E（锁定、撤销、收起） |
| W-04 | 底部缩放、撤销/重做、关闭、保存 | 缩放显示与加减、撤销/重做、本地项目保存、导出 | 部分可用：控制已接到底层内核；PNG/JPEG/WebP、原生项目下载和 JSON 恢复已验证。裁剪后 PNG 会重新解码并作精确 RGBA 断言，JPEG/WebP 会重新解码并核对尺寸；保存项目可从首页最近项目重新打开并恢复画布尺寸。 | RPA + 下载/IndexedDB 断言 |
| W-05 | 工具打开时左侧 280px 参数面板 | 每个工具有标题、参数、取消/应用或关闭 | 部分可用：通用面板、Cutout 模式/容差、Arrange 不透明度/旋转/翻转，以及 Crop 比例、尺寸、90° 旋转/翻转、拉直/应用/取消已完成；其余工具继续对齐 | 工具状态截图 + DOM 断言 |

## 1. 保留的非 AI 编辑功能

| 编号 | 参考工具与细节 | 本地映射 | 当前状态 | 最低验收 |
| --- | --- | --- | --- | --- |
| A-01 | Home：打开图片、新建、临时项目 | 首页本地打开/新建/最近项目 | 部分可用：本地打开、新建、最近项目列表均可到达；已验证从首页最近项目重新打开并恢复保存画布。 | 打开本地图片与恢复项目 |
| A-02 | Arrange：图层选择、名称、透明度、锁定、变换、旋转、翻转、删除、快速加 Frame/Text/Shape | miniPaint 图层、文字、形状、图层移动/复制/合并 | 部分可用：名称、不透明度、常用 Canvas 混合模式、重复、删除、顺序、旋转、翻转、锁定、原子坐标/尺寸/角度变换、文字/形状与基础 Frame 已实现；画布上自由变换待补。锁定层仍可显隐并可撤销，其余修改受 UI/动作双重拦截。 | 图层状态 + 撤销 E2E |
| A-03 | Crop：自由裁剪、宽高、拉直、比例、输出尺寸、旋转/翻转、图片/画布尺寸 | Crop、Resize、Rotate、Flip、Canvas size | 可用：自由裁剪、比例、输出宽高、会话内 90° 旋转/翻转、-45°～45° 拉直、应用/取消均已实现；拉直在应用时以文档坐标重采样当前裁剪区域并作为一条可撤销记录提交，混合矢量图层会安全栅格化。真实拖拽取消不留历史，锁定预检覆盖所有受影响层。 | Crop 尺寸、像素、矢量几何、拉直与撤销 E2E |
| A-04 | Cutout：魔棒、画笔、套索、形状；Keep/Remove；容差、柔化、连续；反选、重置、应用 | selection、magic_erase、erase；需补 Keep/Remove 与面板编排 | 部分可用：矩形、自由套索、椭圆、加减选/反选、Keep/Remove、魔术橡皮和橡皮笔刷均为本地处理；缩放/平移坐标、锁定/旋转保护、鼠标/触摸多指会话隔离、alpha 像素和撤销/重做均已验证。形状类型扩展与专门应用预览待补 | Cutout 触控/像素/撤销 E2E |
| A-05 | Adjust：Auto、B&W、Pop、Color/Light/Details/Scene 参数，Compare/Reset/Apply | Color corrections、Auto adjust、Blur/Sharpen/Desaturate 与效果模块 | 部分可用：Auto、B&W、Pop、Color、Light、Details、Scene 均为本地真实入口；固定图的逐项像素变化/撤销和锁定保护已验证。参数对话框提供预览/取消/应用；Compare、Reset 专门控件仍待补 | 七项 RGBA 像素哈希 + 撤销 E2E |
| A-06 | Effect：预览强度、返回、取消、应用 | Effects browser / 各效果模块 | 可用：本地效果浏览器及各效果预览对话框提供参数强度、取消/应用；重复编辑、撤销/重做能恢复正确参数。锁定或非图片层禁用且底层动作拒绝绕过。 | 效果浏览器、取消、应用/撤销、参数更新、锁定拦截 E2E |
| A-07 | Filter：HDR、Focus/Bokeh、Reflect、Dispersion、Glitch、Colorize | miniPaint 现有滤镜优先映射；缺少的项目列入新增 | 部分可用：六个本地入口均已接通；HDR/Focus-Bokeh 复用 Enrich/Tilt Shift，Reflect/Dispersion/Glitch/Colorize 为本地 Canvas 算法。逐项像素变化、撤销恢复和弹窗后锁定保护已验证；Pixlr 式缩略图预览卡片与更丰富参数待补 | 六项 RGBA 像素哈希 + 撤销 E2E |
| A-08 | Liquify：推移等子工具、Size/Strength/Density、质量预览、关闭 | Bulge/Pinch（WebGL2）为起点；需补子工具与临时预览 | 部分可用：已提供本地 WebGL2 膨胀/收缩，以及基于局部反向采样的推移笔刷、半径/强度/密度；笔触先留在本地临时预览，显式应用才生成一次可撤销历史，取消不写入。锁定、无图层或无 WebGL2 时明确禁用并安全退出旧工具；其他高级液化子工具待补。 | WebGL2 状态、推移笔触、预览取消、应用/撤销/重做 |
| A-09 | Retouch：修复、克隆、模糊/锐化、减淡/加深等 | clone、repair、blur、sharpen、desaturate | 部分可用：克隆、专用修复、局部模糊、局部锐化、局部去色、减淡与加深已接入面板并受锁定保护；修复使用浏览器本地 3×3 邻域中值处理孤立瑕疵，减淡/加深使用本地软边像素笔刷，均有真实像素修改与撤销验证。更复杂的纹理匹配/内容感知修复不在首版范围。 | 局部去色、修复像素基准、减淡/加深历史/撤销 E2E |
| A-10 | Drawing：画笔、填充、笔、形状；颜色、笔刷、大小、柔化、透明度 | brush、pencil、fill、gradient、shape | 部分可用：画笔、铅笔、填充、渐变、形状及颜色/大小/不透明度已接入；锁定层填充无历史写入；实际笔触、柔化和各工具像素基准待补 | 配置/工具激活 E2E；锁定填充 E2E；笔触基准待补 |
| A-11 | Text：添加、字体、填充、对齐、样式、描边、阴影、背景、曲线/变形 | text + 本地系统字体；基础样式可复用 | 部分可用：文字面板已接入系统字体、字号、填充、加粗/斜体/下划线、描边、左/中/右对齐、可撤销阴影/背景，以及 -100～100 本地曲线；曲线以逐字弧线位移/旋转渲染，参数随文字层保存、导出，并可撤销。真实文字层创建、编辑和锁定保护已验证；更丰富的 Warp/变形样式待补。 | 文本图层、编辑、曲线像素改变/撤销、对齐、阴影背景与导出 |
| A-12 | File/Save：PNG/JPEG/WebP、项目文件、关闭 | miniPaint 导出 + IndexedDB 项目 JSON | 部分可用：PNG/JPEG/WebP 下载、原生 JSON 导出/导入、合法空白项目和图片项目恢复已验证；裁剪后 PNG 的精确像素、JPEG/WebP 的可解码性和画布尺寸已验证；保存项目可经 IndexedDB 最近项目重新打开并恢复画布尺寸。 | 可解码导出和项目重开 |
| A-13 | Create collage（首页入口） | 新建 2×2 / 3×1 本地拼贴模板、分格工作台 | 部分可用：模板会建立辅助网格与本地分格面板；可选择分格、拖入或选择本地图片，按 Cover 规则自动裁切并插入对应尺寸的可撤销图层；替换同一分格会替换其图层。更多模板、分格内手势缩放/拖移待补。 | 2×2 分格占位、自动裁切 RGBA、图层尺寸与撤销 E2E |

## 2. 严格排除

| 编号 | 参考入口 | 本地要求 | 验收 |
| --- | --- | --- | --- |
| X-01 | AI Tools 全部子项 | 不渲染、不注册快捷键、不发送模型或远程推理请求 | 源码与网络策略测试 |
| X-02 | AI Cutout | Cutout 中不出现 AI 一键抠图，仅保留手动工具 | DOM/RPA 断言 |
| X-03 | Element | 不渲染 Element 入口、素材库、贴纸/在线素材 API | 源码与 DOM 断言 |
| X-04 | 远程图片 URL、素材搜索、在线字体 | 仅本地文件、浏览器系统字体与本地 IndexedDB | 网络拦截与源码测试 |

## 3. 完整性门槛

- 每个 `A-*` 条目必须从工具轨或 Home 到达，且有一个真实操作结果和撤销/重做证据。
- 每个 `X-*` 条目必须没有 UI 入口、快捷键或请求路径。
- `W-*` 必须与同尺寸参考截图进行视觉比对；P0/P1/P2 差异全部修复才可标记工作台完成。
- 未满足的条目必须保持“缺失/部分可用”，不得用“内核存在”替代验收。
