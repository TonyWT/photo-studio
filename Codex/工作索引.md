# 工作索引

- 2026-07-13｜Pixlr Express 风格、无 AI 网页图片编辑器｜关键词：Pixlr Express、miniPaint、MIT、浏览器本地编辑｜完成可行性评估：推荐 fork miniPaint，裁剪外部服务与 AI/第三方入口，重构首页和编辑器壳层；不要只用 iframe。关键证据：`/tmp/minipaint-assessment` 的 `npm ci --ignore-scripts && npm run build` 构建成功，bundle 约 1.29 MiB。后续入口：先确认是否需要账号和云端项目保存，再写 MVP 规格。
- 2026-07-13｜全功能复刻与 CI/CD 验收｜关键词：功能矩阵、Playwright、视觉回归、Canvas、图像基准、RPA｜明确保留 Home、Arrange、Crop、Adjust、Effect、Filter、Liquify、Retouch、Drawing、Text、图层，移除 AI Tools 与 Element；发布门禁采用确定性单元/端到端/导出图像比对/截图回归。AI 视觉 RPA 只作夜间巡检报告。后续入口：创建“功能-用例-基准图”矩阵后再开始实现。
- 2026-07-13｜Photo Studio 首版实现｜关键词：IndexedDB、miniPaint、browser-use、Playwright、GitHub Pages、无 AI｜已导入 miniPaint MIT 内核，完成本地首页/编辑器双路由、IndexedDB 最近项目、手动 Cutout、系统字体、WebGL2 Liquify 保护、无外部素材/字体入口、RPA 和 CI/Pages/夜间视觉回归工作流。关键文件：`src/js/studio/`、`editor/index.html`、`.github/workflows/`、`docs/feature-matrix.md`。验证：`npm test` 10/10、`npm run build`、`npm run test:e2e` 5/5、`npm run test:e2e:nightly` 7/7、`npm run test:rpa` 通过。后续入口：补齐各滤镜的 PNG 像素基准图。
- 2026-07-13｜编辑器工作台 UI 对照改造｜关键词：Pixlr Express、编辑器工具轨、图层轨、状态栏、手动 Cutout、功能矩阵｜以 1920×878 参考编辑器状态为基准，已完成左侧非 AI 工具轨、中心暗色工作区、右侧窄图层轨、底部缩放/撤销/保存/导出栏和可关闭工具面板；Cutout 已可切换选区/魔术橡皮/橡皮并写入本地容差。关键文件：`editor/index.html`、`src/css/studio.css`、`src/js/studio/editor-shell.mjs`、`docs/pixlr-express-parity-matrix.md`。验证：`npm test` 11/11；Cutout 和工作台 Chromium E2E 通过。后续入口：补全 Arrange/Crop/Adjust/Effect/Filter/Liquify/Retouch/Drawing/Text 面板与固定图片基准，再创建 GitHub 仓库部署。
- 2026-07-13｜工作台真实模块接线与视觉基准更新｜关键词：自动修正、效果浏览器、图层缩略图、Playwright 快照｜已把 Adjust 自动修正和 Effect 浏览器接入新面板，导入图片后右侧层轨显示真实缩略图；视觉快照已从旧 miniPaint 外观更新到新工作台。验证：`npm run test:e2e:nightly` 13/13。关键文件：`src/js/studio/editor-shell.mjs`、`src/js/core/gui/gui-layers.js`、`test/e2e/smoke.spec.mjs`、`test/e2e/visual.spec.mjs-snapshots/editor-chromium-darwin.png`。后续入口：实现图层锁定、Crop 应用/取消与剩余工具的面板化操作。
- 2026-07-13｜工作台关键操作与拼贴入口｜关键词：Arrange、Crop、拼贴、撤销、Playwright｜Arrange 已可复制图层并由底栏撤销恢复；Crop 的应用会真实改变画布尺寸；主页可选择 2×2 / 3×1 本地拼贴模板并在编辑器显示辅助网格。验证：`npm test` 11/11、`npm run test:e2e` 14/14、`npm run test:e2e:nightly` 通过、`npm run build` 通过（编辑器包 1.33 MiB 警告）。关键文件：`src/js/studio/editor-shell.mjs`、`src/js/studio/home.mjs`、`index.html`、`docs/pixlr-express-parity-matrix.md`。后续入口：提交到 `TonyWT/photo-studio` 并由 Pages 工作流发布；继续补全其余矩阵项。
- 2026-07-13｜私有仓库与 Pages 发布核验｜关键词：TonyWT/photo-studio、GitHub Actions、GitHub Pages、private｜已创建私有仓库并通过 GitHub Git Data API 写入与本地提交同一文件树；CI 工作流成功。Pages 部署失败：GitHub API 明确返回当前套餐不支持该私有仓库的 GitHub Pages。关键链接：`https://github.com/TonyWT/photo-studio`、Actions run `29235212630`/`29235212463`。后续入口：用户决定改公开以使用 Pages，或授权 Vercel/Cloudflare Pages 等支持私有源仓库的部署平台。
- 2026-07-13｜已加载图片工作台与 Cutout 扩展｜关键词：3840×2880、26%、图层锁定、裁剪比例、手动 Cutout、视觉回归｜已用固定 3840×2880 本地图片在 1920×878/26% 状态与参考编辑页同状态对照；图层轨补齐缩略图、锁定/撤销、收起，Arrange 补不透明度/旋转/翻转，Crop 补 1:1/4:3/16:9，Cutout 补柔化、全局取样、移除/重置选区且锁定层不可编辑。关键文件：`design-qa.md`、`docs/pixlr-express-parity-matrix.md`、`src/js/studio/editor-shell.mjs`、`test/e2e/visual.spec.mjs`。验证：`npm test` 11/11、`npm run test:e2e` 18/18、`npm run test:e2e:nightly` 21/21、`npm run build` 通过（1.34 MiB bundle 警告）。后续入口：继续逐项完成 A-04~A-13 与 PNG 输出基准；确定私有仓库的部署承载方。
- 2026-07-13｜公开仓库与 Pages 正式发布｜关键词：public、GitHub Pages、tonywt.github.io、部署验证｜用户授权后，`TonyWT/photo-studio` 已改为公开并启用 GitHub Pages（GitHub Actions）；发布工作流 `29237831338` 全绿，浏览器真实加载主页成功。公开地址：`https://tonywt.github.io/photo-studio/`。后续入口：继续按 `docs/pixlr-express-parity-matrix.md` 完成非 AI 功能矩阵；每次合并 main 自动发布。
- 2026-07-13｜原生项目导入结构校验修复｜关键词：project-store、load_json、JSON 导入、图层数据、Playwright｜`isNativeProjectDocument` 现拒绝 `null`、数组或空图层/图像数据项；图层必须有有限 `id` 与字符串 `type`，图像数据必须有有限 `id` 与字符串 `data`，避免首页将会在 `load_json` 中抛错的 JSON 交接到编辑器。验证：`npm test` 13/13、`npm run test:e2e -- --grep '导出|项目'` 8/8；导出 E2E 同时确认 FileSave JSON 保持这些合法字段。关键文件：`src/js/studio/project-store.mjs`、`test/project-store.test.mjs`、`test/e2e/smoke.spec.mjs`。后续入口：如扩展兼容旧版项目，须先以实际旧版导出样本补测试。
- 2026-07-13｜空白画布项目导入 round-trip 修复｜关键词：FileSave、type:null、project-store、空白画布、Playwright｜修正原生校验：图层必须为非空对象且具有限 `id`，`type` 仅允许字符串或显式 `null`；这与 FileSave 导出空白画布的默认层一致。仍拒绝缺失 `type`、空/数组图层，以及不安全图像数据项。验证：先见证 Node 与首页导入 E2E 红灯，后 `npm test` 13/13、`npm run test:e2e -- --grep '导出|项目'` 8/8。关键文件：`src/js/studio/project-store.mjs`、`test/project-store.test.mjs`、`test/e2e/smoke.spec.mjs`。后续入口：若再收紧 schema，必须保留该真实 round-trip 样本。
- 2026-07-13｜Arrange/Effect/Text 深化与 Pages 门禁修复｜关键词：锁定边界、实时效果、Text 对齐、GitHub Pages、CI｜锁定图层现允许可撤销显隐，但禁止像素、滤镜、删除、复制、排序与变换；Effect 支持预览取消、应用、参数更新及撤销/重做；Text 增加左/中/右真实对齐。首轮云端 E2E 发现内核就绪时序，已修复为等待 `app.State` 后重新发布。关键文件：`src/js/studio/editor-shell.mjs`、`src/js/actions/add-layer-filter.js`、`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。验证：本地 60 条 Chromium E2E、13 条单元、生产构建；CI `29260902034` 与 Pages `29260902107` 均成功。公开地址：`https://tonywt.github.io/photo-studio/`。后续入口：继续补 Liquify 临时预览/应用取消、Text 阴影背景、Crop 拉直与图像像素基准。
- 2026-07-13｜Crop 拉直与导出图像验收｜关键词：Straighten、PNG、JPEG、WebP、像素基准、GitHub Pages｜Crop 现提供 -45°～45° 拉直暂存；应用时按文档坐标重采样，作为一条可撤销记录提交，混合图层安全栅格化。导出门禁新增：裁剪后 PNG 重新解码并核对 RGBA，JPEG/WebP 重新解码并核对尺寸。验证：63 条 Chromium E2E、13 条单元和生产构建通过；CI `29263715110`、Pages `29263715162` 成功。关键文件：`src/js/tools/crop.js`、`src/js/studio/editor-shell.mjs`、`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。后续入口：补 Liquify 推移、Retouch 修复/减淡/加深、Text 曲线与拼贴分格工作流。
- 2026-07-13｜Liquify 推移子工具｜关键词：Liquify、Push、WebGL2、局部反向采样、撤销｜液化面板新增推移模式；拖拽在临时图层画布执行带软边半径的本地像素位移，沿用 WebGL2 可用性门槛，应用/取消/撤销重做保持原子历史语义。验证：64 条 Chromium E2E、13 条单元、生产构建通过。关键文件：`src/js/tools/bulge_pinch.js`、`src/js/config.js`、`src/js/studio/editor-shell.mjs`、`test/e2e/smoke.spec.mjs`。后续入口：发布后核验云端 CI/Pages，再补 Retouch、Text 与拼贴矩阵项。
- 2026-07-13｜Retouch 减淡/加深笔刷｜关键词：Dodge、Burn、局部像素、撤销、锁定｜新增 `dodge_burn` 本地工具，以软边强度笔刷局部提亮或压暗 RGB，沿用图片图层、旋转与锁定保护及每笔一条撤销历史。面板可切换减淡/加深和独立强度。验证：66 条 Chromium E2E、13 条单元、生产构建通过。关键文件：`src/js/tools/dodge_burn.js`、`src/js/config.js`、`src/js/studio/editor-shell.mjs`、`test/e2e/smoke.spec.mjs`。后续入口：云端门禁后发布；专用修复笔刷、Text 曲线和拼贴分格仍未完成。
- 2026-07-14｜Text 本地曲线渲染｜关键词：Text、Curve、逐字弧线、Undo、Canvas｜文字工作台新增 -100～100 曲线控制；水平文字逐字沿弧线位移、旋转，参数保存在文字图层，画布/导出使用同一渲染路径且可撤销。验证：先见证曲线控件缺失红测，后 68 条 Chromium E2E、13 条单元与生产构建通过。关键文件：`src/js/tools/text.js`、`src/js/config.js`、`src/js/studio/editor-shell.mjs`、`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。后续入口：发布并核验云端 CI/Pages；继续补 Text Warp、Retouch 专用修复和拼贴分格。
- 2026-07-14｜Retouch 专用修复笔刷｜关键词：Repair、Median、局部瑕疵、Undo、Retouch｜新增 `repair` 本地工具，逐笔使用 3×3 邻域中值消除孤立色斑；保持图片/旋转/锁定边界、临时预览与单笔撤销历史。修饰面板现在同步高亮真实活动工具。验证：先见证缺少入口红测，后确定性 270×270 瑕疵图的中心像素修复/撤销 E2E 与 Retouch 4/4、生产构建通过。关键文件：`src/js/tools/repair.js`、`src/js/config.js`、`src/js/studio/editor-shell.mjs`、`test/e2e/smoke.spec.mjs`。后续入口：提交/发布后继续完成拼贴分格与 UI 同状态对照。
- 2026-07-14｜拼贴分格工作流｜关键词：Collage、2×2、3×1、Cover、分格、自动裁切｜从仅显示辅助线升级为分格工作台：选择分格后可拖入或选择本地图片，浏览器本地按 Cover 裁切为分格尺寸并作为可撤销图层插入；同一格可替换。验证：先见证分格入口缺失红测，后 2×2 的占位状态、400×300 图层尺寸及红蓝横图裁切 RGBA E2E 通过。关键文件：`src/js/studio/editor-shell.mjs`、`src/css/studio.css`、`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。后续入口：补更多模板和分格内调位，再做参考图同状态 UI QA。
- 2026-07-14｜编辑器工具轨同状态视觉校准｜关键词：Pixlr Express、工具轨、选中态、视觉快照、Playwright｜按参考编辑页对照后，去除本地工具轨的青色 3px 选中侧边条，改为中性深灰激活态；新增确定性 CSS 断言，首页视觉基准同步到已包含导入项目与新建拼贴入口的实际界面。验证：先见证选中态断言红灯，后 4 条视觉 E2E 全通过。关键文件：`src/css/studio.css`、`test/e2e/visual.spec.mjs`、`test/e2e/visual.spec.mjs-snapshots/home-chromium-darwin.png`、`docs/pixlr-express-parity-matrix.md`。后续入口：继续补 A-02 画布自由变换、A-05 Compare/Reset、A-10 笔触基准、A-11 Warp 和 A-13 更多模板/分格调位。
- 2026-07-14｜Drawing 画笔像素回归｜关键词：Drawing、Brush、Canvas、Undo、Playwright｜增加 270×270 不透明底图的真实画笔回归：点击画布后会生成笔触图层并改变最终合成像素，底部撤销恢复原 RGBA。关键文件：`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。验证：目标 Chromium E2E 1/1 通过。后续入口：补铅笔、填充、渐变、形状和柔化的固定图像基准。
- 2026-07-14｜Adjust Compare/Reset 本地预览｜关键词：Adjust、Compare、Reset、Popup、Preview、Undo｜预览对话框新增按住 Compare 原图和 Reset 默认参数；放开后恢复调整预览，确认前不写历史。机制复用于本地预览对话框，不涉及远程请求。关键文件：`src/js/libs/popup.js`、`test/e2e/smoke.spec.mjs`、`docs/pixlr-express-parity-matrix.md`。验证：先见证 Compare/Reset 缺失红测，后 Color 对话框的原图切换、默认 Red=12 恢复与无提交历史 E2E 通过。后续入口：继续 A-02 画布自由变换、A-10 其余笔触基准、A-11 Warp 和 A-13 分格内调位。
